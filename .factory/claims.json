[
  {
    "id": "demo-sandbox",
    "claim": "Sample data never mixes with personal records.",
    "where": "Home action, demo banner, README",
    "test": "npm run test:claims -- --grep @claim:demo-sandbox",
    "sandbox": "Fresh browser context at /demo; assert sample records, demo: storage namespace, and discard-on-exit."
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "Home facts, README",
    "test": "npm run test:claims -- --grep @claim:offline-reload",
    "sandbox": "Fresh browser context at /demo, wait for service worker, go offline, and reload."
  },
  {
    "id": "csv-export",
    "claim": "Exports ledger entries as CSV.",
    "where": "Backup screen, README",
    "test": "npm run test:claims -- --grep @claim:csv-export",
    "sandbox": "Fresh /demo ledger; download CSV and assert the header plus one row per sample transaction."
  },
  {
    "id": "encrypted-backup",
    "claim": "Exports a password-encrypted backup.",
    "where": "Backup screen, README",
    "test": "npm run test:claims -- --grep @claim:encrypted-backup",
    "sandbox": "Fresh /demo ledger; download a password backup and assert its encrypted envelope omits sample cleartext."
  },
  {
    "id": "local-records",
    "claim": "Records remain in this browser; no bank login is required.",
    "where": "Home facts, privacy policy, README",
    "test": "npm run test:claims -- --grep @claim:local-records",
    "sandbox": "Fresh /demo flow; assert only product-origin requests and no password/login input."
  },
  {
    "id": "exact-decimals",
    "claim": "Keeps supported money amounts in exact whole cents.",
    "where": "README",
    "test": "npm run test:claims -- --grep @claim:exact-decimals",
    "sandbox": "Fresh /demo ledger; create the maximum accepted USD amount and assert every cent is displayed."
  }
]
